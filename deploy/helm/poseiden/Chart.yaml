apiVersion: v2
name: poseiden
description: POSEIDEN - a Product Owner support tool for Azure DevOps backlog hygiene + flow. Single-replica web instance backed by SQLite on a persistent volume, with optional ingress-layer auth (oauth2-proxy) for standalone (nginx) or enterprise (Istio) clusters.
type: application
version: 0.2.0
appVersion: "0.1.0"
icon: https://raw.githubusercontent.com/andrewiankidd/POSEIDEN/main/assets/logo.png
keywords:
  - azure-devops
  - backlog
  - hygiene
  - observability
  - oauth2-proxy
home: https://github.com/andrewiankidd/POSEIDEN
sources:
  - https://github.com/andrewiankidd/POSEIDEN
maintainers:
  - name: Andrew Kidd
