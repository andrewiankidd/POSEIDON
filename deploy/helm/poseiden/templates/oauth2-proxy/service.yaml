{{- if include "poseiden.deployProxy" . }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "poseiden.oauth2proxy.fullname" . }}
  labels:
    {{- include "poseiden.oauth2proxy.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "poseiden.oauth2proxy.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
{{- end }}
