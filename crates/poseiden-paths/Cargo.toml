[package]
publish = false
name = "poseiden-paths"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "POSEIDEN path resolution - portable-first. Resolves the data dir, config path, DB path and log dir across portable / env-var / OS-standard / default modes so nothing is ever written outside an expected location."

[dependencies]
directories = { workspace = true }

[dev-dependencies]
tempfile = "3"
