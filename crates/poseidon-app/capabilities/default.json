{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permission set for the POSEIDON shell. Core webview plus the shell plugin's open() so the external-link interceptor can hand http(s) URLs (work-item / pipeline links) to the OS browser. Custom commands (get_dashboard, get_tickets, …) declared via invoke_handler in lib.rs are accessible by default; this file covers plugin permissions and any future granular lock-down.",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "shell:allow-open"
  ]
}
