[package]
name = "poseiden-providers"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "POSEIDEN provider integrations - the Provider trait plus the Azure DevOps client. Fetches work items + pipeline runs and normalises them into poseiden-core shapes."

[dependencies]
poseiden-core = { workspace = true }
thiserror = { workspace = true }
reqwest = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
async-trait = "0.1"
base64 = "0.22"
percent-encoding = "2"
tokio = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
